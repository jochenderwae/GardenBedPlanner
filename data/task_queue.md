# Data Engineer Task Queue

Work order for the `data-engineer` agent (`.claude/agents/data-engineer.md`). Maintained by the user (and the main session on the user's behalf) - **the data engineer may check items off and add outcome notes, but may not add new items here.** Improvement ideas the agent notices along the way belong in `suggestions.md` instead.

Run log (most recent first):
- 2026-07-18 (later still): user asked for the WYSIWYG bed editor to start now, in parallel. Reprioritized: two new tasks (plant rendering geometry, example garden data) inserted at the top since the frontend work depends on them; the growing-info monitor/backfill/normalize tasks moved down but are NOT lower priority in the "ignore this" sense - see the note right below the queue about not letting the growing-info process die unnoticed while you're heads-down on 1/2.
- 2026-07-18 (later): handed fully over to the `data-engineer` agent type (now registered - use it directly, no more stand-ins). Item 1 replaced with an explicit monitor-and-complete task for the growing-info run already in flight; a standing "monitor and maintain the tooling" responsibility added below the numbered queue.
- 2026-07-18: queue created. Replaces an ad-hoc background agent that was running task 1 directly - that agent was stopped cleanly (no orphaned process, ingestion phase already complete) before this queue took over, so task 1 resumes rather than restarts.

## Queue

1. [x] **Define and populate plant rendering geometry (`growth_habit`)** - user request (2026-07-18): the WYSIWYG bed editor (frontend work starting now, in parallel with this) needs to draw each plant as something more meaningful than a generic circle - a climbing vine, a tree, a low spreading herb, and a root vegetable all need different visual treatment. This is a data-definition task, not just population: **you decide the category set and mapping**, document your reasoning.
   - **Done 2026-07-19.** Category set: `upright` / `spreading` / `climbing` / `rosette` / `tree` (5 values, closed enum incl. `null`) - full definitions + reasoning for why `spreading` deliberately merges "low woody bush" and "ground-sprawling vine" (both need "wide, low canopy" rendering) are in `data/plant.schema.json`'s field description and `data/etl/populate_growth_habit.py`'s module docstring. Populated via a new `data/etl/populate_growth_habit.py`: deterministic mapping from Trefle's raw `main_species.specifications.growth_habit` where unambiguous (Tree/Vine/Shrub/Subshrub/Graminoid, with a small curated override list for "Vine"-tagged plants that garden practice grows sprawling rather than trellised - sweet potato, water spinach, winter squashes), falling through to a new Ollama inference function (`ollama_resolve.infer_growth_habit`) for everything else - including the single largest bucket, bare `"Forb/herb"` (116 plants), confirmed genuinely ambiguous between upright/rosette/spreading by spot-checking real cached data (tomato vs. carrot vs. strawberry are all bare `"Forb/herb"` in Trefle but obviously different shapes). Result: 349/359 plants populated (249 via Ollama, 100 deterministic from Trefle), 10 correctly declined rather than guessed (`data/growth_habit_unmatched.jsonl`) - 5 are mushrooms/an aquatic plant/a fern that don't fit any of the 5 categories, and 2 (`watermelon`, `ferry-morse-contender-bush-bean`) turned out to have pre-existing wrong `botanical_name` values from an earlier ETL run, caught because Ollama correctly refused to categorize a plant against the wrong species - see `data/suggestions.md` for that finding and others (backend/frontend integration note, a `cherry.json` height data-quality flag, a couple of dangling `companion_slug` near-misses). Also fixed the stale `data/CLAUDE.md` note this task's own queue entry flagged (Trefle's `growth_habit` lives under `main_species.specifications`, not top-level). Every populated value is source-tagged in `data_sources` (`trefle-growth-habit` or `ollama-growth-habit-inference`) per the task's instruction to keep the two distinguishable. Ran concurrently with task 3 (growing_info) finishing up in the background per this reprioritization's own instructions - mitigated the shared-file-write risk with a re-read-fresh-before-write pattern plus a "skip if modified in the last 5s, retry at the end" hot-file guard in the script (see its docstring's "Concurrency note"); no evidence of any clobbered write (growing_info actually finished, 140/140, before this task's write pass reached any of its 140 in-scope plants).
   - Real source data confirmed present (checked directly, don't waste time rediscovering this): `data/.cache/trefle/<slug>.json` → `main_species.specifications.growth_habit` - a comma-separated free-text field with real USDA-style values already cached for ~215 of 298 plants that have Trefle data at all (`"Forb/herb"`, `"Tree"`, `"Shrub"`, `"Vine"`, `"Subshrub"`, `"Graminoid"`, and combinations like `"Vine, Forb/herb"`). `main_species.specifications` also has `average_height`/`maximum_height` (mostly null in what's cached, don't rely on it) - `Plant.height_cm`/`spread_cm` (already in `data/plants/*.json`, sourced elsewhere) are the reliable size fields, this task is about *shape category*, not size.
   - **Note this corrects a stale claim in `data/CLAUDE.md`**: it says Trefle has `growth_form`/`growth_habit`/`shape_and_orientation` at the top level of `main_species` - those keys exist there but are always `null`; the real data is one level deeper, under `main_species.specifications`. Fix that note while you're in there.
   - Design a small category set suited for rendering (not Trefle's raw combinatorial strings) - something like bush/spreading vs. vine/climbing vs. upright vs. tree vs. rosette is a reasonable starting point, but it's your call; write down why you chose what you chose.
   - For plants with no Trefle cache or empty `growth_habit`, infer from `description`/`botanical_name` via Ollama (same conflict/inference pattern already used elsewhere in this pipeline - see `data/etl/ollama_resolve.py`), and log what you inferred vs. what came from Trefle so the two are distinguishable later.
   - Add the field to `data/plant.schema.json` (with a description of the category set) and populate `data/plants/*.json`. Log anything you couldn't confidently categorize rather than guessing (same `*_unmatched.jsonl` convention as the rest of this pipeline).
   - **Out of your scope, explicitly**: adding a matching `backend/app/models/plant.py` column, migration, API field, or frontend rendering logic - that's application code, not data. Note in `data/suggestions.md` what you named the field and its category values so whoever builds that (the user/main session) doesn't have to reverse-engineer it from the JSON.

2. [x] **Generate a realistic example garden layout** (`data/example_garden.json`) - user request (2026-07-18), for the frontend WYSIWYG editor to have something real to render while it's being built, and later for the backlogged "load example data on install" feature (see `product-owner/BACKLOG.md`'s Plant database section). Depends on task 1 for variety, but don't block hard on it if task 1 is taking a while - a reasonable default shape/size for plants without `growth_habit` yet is fine.
   - **Done 2026-07-19.** Built via a new `data/etl/generate_example_garden.py` (run with `uv run python -m etl.generate_example_garden` from `data/`) rather than hand-written, so the placement math (grids of individual plant instances per bed, spaced off each plant's real `spread_cm`/`row_spacing_cm`, capped at 6 instances/plant for readability) is reproducible and re-runnable if plant data changes. 12 beds: 4 large planters (one greenhouse, holding tomatoes/peppers), 3 small planters (roots/leafy greens per the task's own guidance), 2 compost bins (no plantings), 1 berry row (raspberry/blueberry/redcurrant/gooseberry/sunflower), 2 fruit tree beds (cherry, pear). Every bed's plant combination was checked against real `companions[].relationship` data with a throwaway script before finalizing - zero `bad`-relationship conflicts in any bed, several combinations backed by an explicit `good` entry (tomato+basil, acorn-squash+borage, cucumber+beans-bush, spinach+lettuce). Full reasoning (bed-dimension interpretation from root `CLAUDE.md`'s ambiguous "70x70x200cm" phrasing, coordinate convention, per-bed plant choices, placement algorithm) is in the generator's module docstring and the output's own `_notes` field. Passes `etl.verify_garden` cleanly (12 beds, 620cm x 650cm bounding box, no overlaps/out-of-bounds/unknown-slug problems). One real data gap surfaced and logged to `suggestions.md`: no dedicated "sour cherry" (`Prunus cerasus`) plant record exists, so the generic `cherry.json` (sweet cherry, `Prunus avium`) was used as the closest real match for that bed.
   - Model the actual physical garden described in root `CLAUDE.md`: 4 large planters (70×70×200cm, one with a greenhouse), 3 small planters (30×70×70cm), a berry row (raspberry, blueberry, redcurrant, gooseberry, sunflowers), 2× 1m³ compost bins, 2 fruit trees (sour cherry, pear). There's no fixed real layout recorded anywhere - invent a sensible non-overlapping arrangement (e.g. a grid with realistic path gaps between beds) and say so.
   - Match the real `Bed` model's fields exactly (`backend/app/models/bed.py`): `name`, `bed_type` (`large_planter`|`small_planter`|`berry_row`|`compost_bin`|`fruit_tree`), `width_cm`, `length_cm`, `height_cm`, `has_greenhouse`, `pos_x`, `pos_y`, `notes` - omit `id` (server-assigned). This isn't imported into Postgres by anything yet (no `BedPlanting` model exists - see the backlog item), it's a dev/demo fixture for now, but keeping the shape identical means whoever writes that importer later doesn't have to transform it.
   - For each bed, add a `plantings` array (not a real table yet, just illustrative JSON for the frontend to render): real plant slugs from `data/plants/*.json`, positioned in local bed coordinates (`x_cm`/`y_cm` relative to the bed's own `width_cm`/`length_cm`), spaced realistically using each plant's `row_spacing_cm`/`spread_cm` so beds don't look absurdly over- or under-planted. Pick plants that actually fit each bed's size and purpose (root veg/leafy greens in small planters, trees only in `fruit_tree`-type beds matching the 2 real trees, etc.). Where it's easy, avoid placing two plants with a `bad` `companions[].relationship` to each other in the same bed - don't spend a lot of time on this, just don't actively contradict data that's already sitting right there in each plant's own JSON.
   - Log your bed layout and plant choices' reasoning briefly in the file itself (a top-level `_notes` field is fine) or in your outcome note here - this is a judgment-call artifact, not a mechanical transform, so leave a trail for why you chose what you chose.
   - Before checking this off, run `claudeTools/data_verify_garden.ps1` (from the repo root, or `uv run python -m etl.verify_garden` from `data/`) - validates bed schema shape, planting bounds, bed overlap, and that every `plant_slug` is real. Exits non-zero on any problem; re-run after any change to `etl.generate_example_garden` or the garden data itself.

3. [x] **Monitor and complete the growing-information ingestion run** - a `uv run python -m etl.growing_info.run` process is already running as a genuinely OS-detached Windows process (started via PowerShell `Start-Process`, not tied to any tool-tracked background job, so it survives session/job-lifetime boundaries that killed two earlier tracked-job attempts). See it through to completion - but since it's passive background monitoring, not active work, don't let tasks 1/2 above starve it: do a liveness check before and periodically during 1/2, not just when you formally reach this item.
   - **Done 2026-07-19.** Did liveness checks before starting task 1, and again partway through (both times confirmed alive and `plant_pass_done` climbing). By the time task 1's design work was finished and its own write pass was about to start, PID 3380 had already exited on its own and `etl.state_report` showed all 140/140 plants with all 4 passes done (consolidate/extract/crosscheck/surface) - confirmed against `.growing_info_run.out.log`'s final line (`[140/140] passes done for yellow-onion`, matching the state DB exactly, no errors in `.growing_info_run.err.log`). So task 1's data/plants/*.json write pass never actually raced a still-running task 3 in practice, though the hot-file/re-read-fresh mitigation described in task 1's note was in place regardless. Canonical output logs from this run: `data/growing_info_unmatched.jsonl` (29 entries - book sections that didn't match any plant), `data/growing_info_crosscheck_log.jsonl` (104 entries - text vs. structured-data inconsistencies, e.g. old-book row spacing in feet not matching modern cm values), `data/growing_info_interesting_log.jsonl` (611 entries - observations that didn't map to an existing field). Spot-checked all three: no systematic per-source failure pattern, the crosscheck entries read as genuine historical-vs-modern-practice differences rather than extraction bugs. 611 "interesting" entries is a real volume worth a deliberate review pass later (noted in `suggestions.md`) - out of scope to act on here per this task's own design (`data/CLAUDE.md`: "Log it for review... not automatic").
   - Process was PID 3380 as of 2026-07-18 23:1x (`python.exe` under `uv`) - re-check with `Get-Process`, the PID may have changed if it was relaunched since. Logs to `data/.growing_info_run.out.log` / `.err.log` (may show nothing for a long time - Python's stdout is block-buffered when redirected to a file, so lack of log output doesn't mean it's stuck).
   - Check real progress via the state DB, not the log: `claudeTools/data_run_module.ps1 etl.state_report` (from the repo root) prints exactly this - `plant_pass_done` distinct-plant count out of 140 total. Last checked 2026-07-18 23:5x: 26/140 and climbing steadily.
   - Once `plant_pass_done` covers all 140 plants (4 rows each - consolidate/extract/crosscheck/surface) it's done. If the process dies before finishing: it's safe to just re-run the same command (fully resumable) - if you relaunch it, use the same detached `Start-Process` pattern (via `PowerShell`, not `Bash` - see "Hard boundary" above), not the Agent/Bash tool's own `run_in_background`, since that's what got killed twice already.
   - Full pipeline design in `data/CLAUDE.md` (search "growing_information") and `data/etl/CLAUDE.md`.

4. [x] **Backfill missing family/genus** - for plants no source gave a family/genus at all (54 of 359 after the main ETL run, mostly cultivars), look up taxonomy via Wikipedia/Wikidata, grouped by genus (derived from `botanical_name`) so cultivars sharing a genus share one lookup. See `data/etl/sources/wikipedia_taxonomy.py` and the `wikipedia-taxonomy` row in `data/CLAUDE.md`'s source table.
   - **Done 2026-07-19.** 51/54 resolved (22 genera). First run resolved 20 genera/48 plants but 2 genera failed - `Prunus` (rainier-cherry) and `Pyrus` (green-bartlett-pear, red-danjou-pear) - which was surprising for two extremely common genera, so investigated rather than accepting it: found and fixed a real bug in `etl/sources/wikipedia_taxonomy.py`. Both genera's Wikidata parent-taxon chain resolves to Rosaceae (Q46299), but that item has **no `"en"` label at all** - only `"mul"` (Wikidata's pseudo-language for values that don't vary by language, increasingly used for scientific/taxonomic names since ~2023) plus dozens of other specific languages. `_wikidata_entity()` only requested `languages: en`, and `_label()` only read `labels.en`, so a perfectly good family match silently produced `None`, misread as "unresolvable" rather than "found, but under a different label key." Fixed by requesting `en|mul` and falling back to `mul` in `_label()`. Cleared the 68 stale cached `data/.cache/wikipedia-taxonomy/entity_*.json` responses (fetched under the old `en`-only param, so didn't have `mul` cached and would have kept serving the bug from disk) and re-ran - both genera resolved cleanly on the second pass. Remaining 3 unresolved, all legitimately (see `data/suggestions.md`): `french-breakfast-radish` (abbreviated genus `"R. raphanistrum..."`, correctly declined by design), `komatsuna` (lowercase `botanical_name` breaks genus derivation by design), `snow-man-carrot` (genus value `"Carotte"` is French for "carrot", not a real genus - nonsense input, correctly declined). `data/taxonomy_backfill_unmatched.jsonl` has 3 stale lines from the first (pre-fix) run for the now-resolved `rainier-cherry`/`green-bartlett-pear`/`red-danjou-pear` - left in place per this pipeline's audit-trail convention (append-only, same as `conflicts_log.jsonl`) rather than rewritten, since a fix landing mid-run and superseding an earlier failed attempt is itself part of the trail.
   - Run: `claudeTools/data_run_module.ps1 etl.backfill_taxonomy` (from the repo root), or `cd data && uv run python -m etl.backfill_taxonomy`.
   - Writes to `data/plants/*.json` (adds `family`/`genus` + a `data_sources` entry) and logs unresolved genera to `data/taxonomy_backfill_unmatched.jsonl`.
   - **Status as of 2026-07-18**: code written and smoke-tested against the live Wikipedia/Wikidata APIs (confirmed correct for `Solanum`→Solanaceae, `Phaseolus`→Fabaceae, `Capsicum`→Solanaceae; confirmed nonsense genus values like `"Carotte"`/`"R."` fail safely to unmatched rather than guessing) - not yet run against the real `data/plants/*.json` files. Hadn't been run because `data/plants/*.json` was still being actively written by task 3 - safe to run once task 3 is done.

5. [x] **Normalize common_name capitalization** - sources disagree wildly on casing (`"bitter orange"`, `"Adjuma pepper"`, `"Matariki Taewa Potato"` all seen for real); normalize every `data/plants/*.json`'s `common_name` to consistent Title Case.
   - Run: `claudeTools/data_run_module.ps1 etl.normalize_common_names` (from the repo root), or `cd data && uv run python -m etl.normalize_common_names`.
   - `data/etl/normalize.py`'s `title_case_plant_name` already wired into `data/etl/export.py`, so this is a one-off catch-up pass for files exported before that existed, not something future runs need repeated.
   - **Status as of 2026-07-18**: code written and unit-tested (handles hyphenated names, apostrophes, ALL-CAPS input correctly) - not yet run against real data, for the same reason as task 4.
   - **Done 2026-07-19.** First run: 52 plants updated. Two of those looked wrong (`"Holy Basil (Tulsi)"` -> `"...(tulsi)"`, `"Melon (Honeydew/Specialty)"` -> `"...(honeydew/specialty)"`) - both were *already correctly cased in the source*, and the normalizer was actively mangling them, not just failing to improve them. Root cause: the function capitalized literal index `[0]` of each token, and `'('.upper()` is a no-op, so a token starting with punctuation never reached its real first letter. Fixed `_capitalize_word` in `data/etl/normalize.py` to find and capitalize the first actual letter instead of always index 0. Re-ran: 2 corrected, but `melon-2` was still off (`"...(Honeydew/specialty)"` - lowercase after the `/`) since `/` wasn't a split boundary at all, so only the very first letter of `"honeydew/specialty"` ever got capitalized; added `/` to `_WORD_SPLIT_RE` and re-ran again - both `holy-basil` and `melon-2` now match their original pre-normalization values exactly (net-zero diff vs. the last commit). A further run confirmed 0 changes (idempotent). Two smaller, real cosmetic edge cases noticed but not fixed (need a curated exception list, not a mechanical fix) - logged to `suggestions.md`: acronyms getting lowercased (`"SFG"` -> `"Sfg"`), and proper nouns after an apostrophe staying lowercase (`"D'Anjou"` -> `"D'anjou"`).

6. [x] **Import `growing_information` into Postgres** - user request (2026-07-19), found during a live deploy check: `backend/app/scripts/import_plants.py` upserts `data_sources`/`seed_info`/`periods`/`companions`/`bedding_needs`/`pest_interactions` per plant, but never `PlantGrowingInformation` - confirmed live, `GET /api/plants/tomato` on `garden-planner-dev` shows real `data_sources` entries tagged `project-gutenberg` but an empty `growing_information: []`, even though `data/plants/tomato.json` has real entries and the table (`PlantGrowingInformation`, migration `6293e1b8248d_add_plant_growing_information`) already exists. This is exactly the "matching table exists, importer doesn't populate it" case in your `.claude/agents/data-engineer.md`'s new "Keeping the importer in sync" section - first real use of that boundary exception, follow it as written.
   - **Done 2026-07-19.** Added a `growing_information` delete-then-reinsert block to `import_satellites()`, same shape as the adjacent `bedding_needs`/`pest_interactions` blocks (`session.exec(delete(...).where(plant_slug == slug))` then `session.add(PlantGrowingInformation(plant_slug=slug, **gi))` per entry). Confirmed the JSON entry keys (`text`, `source_url`, `attribution`, `copyright_status`, `record_type`, `generic_for_species`, per `data/etl/growing_info/storage.py`'s `add_growing_info_entry`) match `PlantGrowingInformation`'s model fields 1:1, so `**gi` unpacking works directly, no field-name translation needed (unlike `PlantCompanion`'s `companion_slug`/`companion_plant_slug` mismatch). `claudeTools/backend_lint.ps1` (ruff, all checks passed) and `claudeTools/backend_test.ps1` (1 passed) both clean - neither needs a live Postgres connection. **Not run against real data** - no Postgres reachable from this machine; whoever next runs `import_plants.py` against `garden-planner-dev` (or wherever it's deployed) should confirm `GET /api/plants/tomato`'s `growing_information` array is populated afterward, mirroring the exact live check that surfaced this gap. No `data/plants/*.json` changes - pure importer wiring.
   - Mirror the existing per-satellite pattern in `import_satellites()` exactly (see how `bedding_needs`/`pest_interactions` each do a `delete-then-reinsert` block) - a `growing_information` array entry's JSON keys (`text`, `record_type`, `source_url`, `attribution`, `copyright_status`, `generic_for_species`) already match `PlantGrowingInformation`'s model fields 1:1 (see `data/etl/growing_info/storage.py`'s `add_growing_info_entry` for the exact shape being written), so this should be a small, boring addition, not a redesign.
   - Verify with `claudeTools/backend_lint.ps1` and `claudeTools/backend_test.ps1` (both run fine without a live Postgres). You cannot run the importer against real data yourself - note that clearly in your outcome comment so whoever deploys next (the user/main session) knows to run it.
   - Stage/commit per the updated "Committing work to git" section: `git add backend/app/scripts/import_plants.py` alongside whatever `data/` paths this task touches (should be none, this is a pure importer change - no `data/plants/*.json` writes expected).

7. [x] **Resolve the last 3 family/genus gaps using real research, not just automated re-derivation** - user request (2026-07-19). Task 4's automated genus derivation (`wikipedia_taxonomy.genus_from_botanical_name`) correctly declined these 3 rather than guess, but all 3 are fixable with a few minutes of real lookup - the underlying `botanical_name` values are just dirty in a way the automation can't safely paper over on its own:
   - **Done 2026-07-19.** All 3 hypotheses in this queue item confirmed correct via real lookups (not just re-trusted). No `WebSearch`/`WebFetch` tool was actually attached in this session despite the note above saying they're confirmed working (regression - see report) - worked around by hitting Wikipedia's own API and DuckDuckGo's HTML search directly via `PowerShell`'s `Invoke-RestMethod`/`Invoke-WebRequest`, which counts as the same "real research" the task asked for. `french-breakfast-radish`: Wikipedia's Radish article gives the exact binomial `Raphanus raphanistrum subsp. sativus` (alongside the more common synonym `Raphanus sativus`) - genus `Raphanus`, family `Brassicaceae` confirmed. `komatsuna`: Wikipedia's Komatsuna article confirms `Brassica rapa var. perviridis` (the cached `botanical_name` had a typo, `pervidis`) and that the parenthetical cultivar-group notation doesn't change the answer - same species as turnip/mizuna/napa cabbage/rapini - genus `Brassica`, family `Brassicaceae` confirmed. `snow-man-carrot`: no Wikipedia page for the cultivar itself, but DuckDuckGo search surfaced it as a real F1 hybrid white carrot sold by multiple real seed companies (Territorial Seed, Stokes Seeds, Wong Sam Hing Seeds, nunhems) - an Imperator-type `Daucus carota`, confirming the file's existing `companions[]` entry pointing at `plants/carrot.json` was already correct too - genus `Daucus`, family `Apiaceae` confirmed. Fixed each dirty `botanical_name` to a clean binomial (matching sibling records' existing conventions - e.g. `plants/carrot.json`'s own `Daucus carota`), logged a `manual-research`-attributed `data_sources` entry on each citing what was verified, then re-ran `etl.backfill_taxonomy` so the existing automated pipeline resolved family/genus the normal way (added its own `wikipedia-taxonomy` `data_sources` entry per plant, same as the other 51 genera task 4 resolved) - chose this over hand-setting family/genus directly since the underlying `botanical_name` values really were just dirty, not unresolvable, and fixing them upstream is more useful than patching around them. Confirmed 0 plants now missing family/genus (was 3), and all 3 files still validate against `plant.schema.json`.
   - `french-breakfast-radish`: `botanical_name` is the abbreviated `"R. raphanistrum..."` - the genus is almost certainly `Raphanus` (radish), family `Brassicaceae`. Verify, don't assume - `WebSearch`/`WebFetch` are now confirmed working for you (see the `.claude/settings.json` fix), use them.
   - `komatsuna`: `botanical_name` is lowercase, `"brassica rapa (pervidis / komatsuna)"` - likely `Brassica rapa`, family `Brassicaceae`, but confirm rather than pattern-match blindly (the parenthetical cultivar-group notation is unusual, worth checking it doesn't change the answer).
   - `snow-man-carrot`: `botanical_name` is `"Carotte Hybride Snow Man"` - `"Carotte"` is French for carrot, not a genus. Likely genus `Daucus`, family `Apiaceae`, but verify.
   - Your call how to fix each: normalize the dirty `botanical_name` to a clean binomial and re-run `etl.backfill_taxonomy` so the existing pipeline resolves it normally, or set `family`/`genus` directly with a `data_sources` entry attributed to manual research (there's already a precedent for this exact pattern - `data/etl/export.py`'s `_MANUAL_BOTANICAL_NAME_OVERRIDES` for `kamokamo`, same idea: automation correctly declined, a human/researched answer fills the gap, and it's tagged so it's not confused with an automated source). Whichever you pick, keep it consistent with how the rest of this pipeline distinguishes automated vs. manually-verified values.

8. [ ] **Fix bogus `"You Can Grow Anything"` common_name on 5 plants** - user request (2026-07-19): `phacelia`, `mapacho`, `lemon`, `curly-dock`, and `carrots` all have `common_name: "You Can Grow Anything"` - not a real plant name (it's a gardening TV show/book title that evidently got scraped as if it were the common name). Already confirmed for you: all 5 have a correct `slug` and a correct, clean `botanical_name` (e.g. `phacelia` → `Phacelia tanacetifolia`, `carrots` → `Daucus carota`) - only `common_name` is wrong, so this is narrowly fixable without touching anything else.
   - Derive/verify the real common name from each plant's `botanical_name` (cross-check against the `slug` too, but don't just de-slugify blindly - `slug` isn't always a clean name, e.g. `carrots` is plural and may itself be a near-duplicate of the existing `carrot` plant, already flagged once in `data/suggestions.md`'s dangling-`companion_slug` finding - worth a quick check whether `carrots` and `carrot` should actually be the same plant while you're in there, though don't merge/delete records without flagging it here first if so).
   - **Find the root cause, not just the 5 symptoms**: check `data/conflicts_log.jsonl` for these 5 slugs' `common_name` conflict entries - if `"You Can Grow Anything"` won an Ollama-mediated conflict resolution against a real candidate name from another source, that's a resolve-quality bug worth understanding (which source contributed the bad value, why Ollama picked it) so it doesn't silently recur on a future full `etl.run`. If it's a single-source data-quality issue (e.g. one Homesteader Labs crop file has this for exactly these entries), say which source and whether other not-yet-noticed plants could be affected the same way - don't assume these 5 are the only ones without checking.

Tasks 1/2/4/5/7/8 all do a full read-modify-write pass over `data/plants/*.json`, same as task 3's ingestion - don't run any two of these concurrently against the same files. `claudeTools/*.ps1` (repo root, one level up from `data/`) has stable wrappers for the commands above - prefer them over typing raw `cd data; uv run ...` (see `claudeTools/README.md`).

## Standing responsibility (ongoing, not a queue item - don't check this off)

**Monitor and maintain the data ingestion tools.** Beyond working the numbered queue above, keep `data/etl/` (and `data/etl/growing_info/`) in good working order on an ongoing basis:

- Watch for scripts failing, hanging, or producing obviously-wrong output when you run them; fix small bugs directly (consistent with "The queue" section's rule on in-scope code fixes) rather than leaving them broken for the next run.
- Keep the resumability contract intact - every pipeline here depends on its SQLite state DB accurately reflecting reality (see `data/etl/CLAUDE.md`). If you ever find state that's inconsistent with the actual `data/plants/*.json` files (e.g. a stage marked done but the data's missing), investigate before trusting either side, and note what you found.
- Periodically sanity-check the unmatched/conflict logs (`data/growing_info_unmatched.jsonl`, `data/taxonomy_backfill_unmatched.jsonl`, `data/conflicts_log.jsonl`) for patterns worth surfacing (e.g. a source that's failing systematically, not just on individual plants) - that's a `suggestions.md` entry, not something to fix unilaterally if it's bigger than a small code fix.
- This is a standing duty, not something with a finish line - it doesn't block you from working the numbered queue, and finishing the numbered queue doesn't mean you're done with this either. If there's genuinely nothing to do, say so in your report rather than inventing busywork.
