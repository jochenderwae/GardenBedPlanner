# Code review log

Dated, append-only log for the `code-reviewer` agent (`.claude/agents/code-reviewer.md`). One entry per pass: area reviewed, what changed, what was deferred to a backlog ticket (and its number). This agent has no standing commit authorization, so most entries describe working-tree changes left for the user to review, not already-committed history.

<!-- Newest entries at the top. -->
